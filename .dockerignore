# 이미지에 들어가면 안 되는 것들.
#
# `data/` 를 뺀 것이 중요하다 — 검수 결과·질문 이력이 쌓이는 곳이라 **볼륨**으로 붙인다.
# 이미지에 구워 넣으면 컨테이너를 새로 만들 때마다 그동안 승인한 QA가 사라진다.

.venv/
__pycache__/
*.pyc
.pytest_cache/
.git/
.env

data/
퍼블/
docs/
tests/
참고/
*.md
*.bat
