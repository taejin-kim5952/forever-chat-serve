# 이어서 작업할 내용 — openapi-chat-serve

> **이 문서를 읽는 AI에게**: 이 저장소를 처음 받은 상태라고 가정하고 쓴 인수인계 문서입니다.
> 아래 순서대로 읽으면 지금까지의 판단 근거와 남은 일을 알 수 있습니다.
> 코드에는 "왜 이렇게 했는지"가 주석으로 남아 있으니, 고치기 전에 해당 파일 상단 주석을 먼저 보세요.
>
> **작성 시점**: 2026-08-14 / **작성자**: Claude (이전 세션)
> **갱신**: 2026-08-15 — ①QA 생성 · ②품질 평가 · ③관리자 화면(8탭·세션 로그인) · ④질문 분석이
> 모두 들어갔습니다. 바뀐 곳: §2 · §3-2 · §4 · §5-9~5-17 · §6.
> **갱신**: 2026-08-17 — 임베딩이 Ollama 에서 **ONNX**로 옮겨 가 운영 컨테이너가 하나가 됐고
> (9-2), **배포 이미지를 만들어 띄우는 것까지 확인**했습니다(§6-⑤). 남은 것은 **개발 서버 반입과
> 실사용 검증**입니다.

---

## 0. 30초 요약

KT OpenAPI Portal "API Manager" 사용법을 안내하는 **사내 FAQ 챗봇**입니다.

이전 버전(`openapi-chat`)은 질문마다 LLM이 답변을 생성해서 **1건에 1~3분**이 걸렸습니다.
운영 서버에 GPU가 없어 못 올립니다. 그래서 구조를 바꿨습니다.

```
이전:  질문 → 문서 검색 → LLM이 그 자리에서 생성              1~3분
지금:  [사전] 문서 → [질문 모델] → [답변 모델] → [채점 모델] → 사람이 검수  (스튜디오 PC)
       [실시간] 질문 → 가장 가까운 질문을 찾아 그 답변 반환    0.6~0.9초  ← 측정값
```

**운영에서는 LLM을 부르지 않습니다.** 남는 AI는 임베딩 모델 하나뿐입니다.
사전 단계에서만 모델을 셋 씁니다 — 역할마다 잘하는 모델이 다르고, **답을 쓴 모델이 자기 답을
채점하면 점수가 후해지기** 때문입니다(5-19 · 5-20).

현재 상태: **문서 등록 → 생성 → 채점 → 검수 → 답변이 한 줄로 이어집니다.** 진행 상황은
어느 탭에 있든 진행 현황의 작업 현황판에서 보입니다(2026-08-16). 배포 이미지도 만들어
띄워 봤습니다(2026-08-17). 남은 것은 **개발 서버 반입과 실사용 검증**입니다.

지금 필요한 것은 코드가 아니라 **사람이 앉아서 검수하는 일**입니다. 문서에서 QA를 뽑아
승인하면 그만큼 챗봇이 답합니다.

---

## 1. 받아서 바로 실행하기

```bash
git clone <repo> && cd openapi-chat-serve
python -m venv .venv
.venv\Scripts\activate          # Windows / Linux는 source .venv/bin/activate
pip install -r requirements.txt

copy .env.example .env          # ADMIN_PASSWORD 를 반드시 바꿀 것

# 임베딩 모델 (약 1.1GB)
ollama pull bge-m3

# 벡터 저장소는 저장소에 없다(파생물). 아래로 만든다.
python scripts/seed_demo_qa.py --force

python -m uvicorn app.main:app --reload --port 18100
```

> **Python 은 3.11 을 쓰세요.** 3.12/3.14 에서는 `chroma-hnswlib` 휠이 없어 C++ 컴파일러를
> 요구합니다(Windows 에서 빌드 도구 없이 설치가 실패합니다). 3.11이 없으면 `py install 3.11`.

**Windows 에서는 위 과정을 `실행.bat` 더블클릭 하나로 대신할 수 있습니다.** 가상환경·벡터
저장소가 없으면 만들고, 서버가 뜨면 브라우저를 엽니다. 끄는 것은 `stop.bat` 입니다
(18100 포트를 잡은 프로세스만 종료합니다). 판단과 안내 문구는 `scripts/launch.py` ·
`scripts/stop.py` 에 있습니다 — **`.bat` 에는 한글을 쓰지 마세요**(5-14).

| 주소 | 설명 |
| --- | --- |
| `http://localhost:18100/` | 챗봇 화면 |
| `http://localhost:18100/admin` | **관리자** — 사이드바 10항목. 진행 현황(홈) · 검수 · QA 인덱스 … |
| `http://localhost:18100/health/ready` | 준비 상태 |
| `http://localhost:18100/docs` | API 문서 (FastAPI 자동 생성) |

### 동작 확인

```bash
python -m pytest -q          # 59개 통과 (Ollama 없이 돕니다 — 임베딩·LLM 모두 가짜로 바뀝니다)
```

챗봇이 답하는지:

```bash
curl -X POST http://localhost:18100/api/ask \
  -H "Content-Type: application/json" \
  -d "{\"question\":\"API 등록하는 방법 알려줘\"}"
```

---

## 2. 지금 무엇이 되고 무엇이 안 되나

### 되는 것 ✅

| 기능 | 위치 | 비고 |
| --- | --- | --- |
| 챗봇 화면 전체 | `app/static/chat.*` | 퍼블 산출물 이식 완료, 서버 연동 완료 |
| 질문 → 답변 검색 | `app/pipeline/retrieve.py` | 3갈래 분기 전부 동작 |
| QA 저장소 · 벡터 색인 | `app/qa/` | 저장·검수·재색인 |
| 문서 색인 · 관련문서 | `app/ingestion/` | `related_docs` 카드 |
| 질문 이력 + CSV 내려받기 | `app/api/admin_questions.py` | 운영에서 계속 살아 있는 기능 |
| 카테고리 (대분류 5 / 48개) | `data/categories.json` | 퍼블 더미에서 변환 |
| Basic 인증 | `app/core/auth.py` | 관리자 전 구간 |
| serve/studio 모드 게이팅 | `app/core/config.py` | 문서 편집·QA 생성이 serve에서 403 |
| **QA 사전 생성** (studio) | `app/studio/generate.py` + `scripts/generate_qa.py` | 2026-08-15. 문서·질문 → 초안(pending) |
| **관리자 화면** | `app/static/admin.*` · `/admin` | 2026-08-16. 사이드바 10항목 · 진행 현황 · 검수 · 설정 하위탭. 퍼블 산출물 이식 + API 연결 완료 |
| **진행 현황 집계** | `app/pipeline/status.py` · `GET /api/admin/pipeline/status` | 2026-08-16. 6칸 + 병목 하나 + 흐름 요약 |
| **납품처 프로필** | `app/core/profile.py` · `data/profile.json` | 2026-08-16. 조직명·서비스명·도메인 소개·언어를 파일로 분리 |
| **세션 로그인** | `app/core/session.py` · `app/api/admin_auth.py` | 2026-08-15. 로그인 모달(`#authModal`). Basic 인증도 병행 |
| **질문 분석 / 군집** (탭 ②) | `app/pipeline/analytics.py` | 2026-08-15. LLM 없이 동작 — 운영에서도 돕니다 |
| **품질 평가** (탭 ⑦, studio) | `app/studio/evaluate.py` | 2026-08-15. 임계값별 적중·오매칭 표까지 |
| **폴더 업로드** (⑤, studio) | `app/ingestion/doc_upload.py` · `POST /api/admin/docs/upload` | 2026-08-16. 파일 이름이 문서 ID. 화면이 몇 건씩 나눠 보냄 |
| **역할별 모델** (⑥, studio) | `ModelRoles` (`app/studio/generate.py`) | 2026-08-16. 질문·변형 / 답변 / 채점을 따로 고름 |
| **채점(판정자)** (⑥, studio) | `app/studio/judge.py` | 2026-08-16. 1~5점 + 이유. 4점 미만은 반영에서 자동 제외 |
| **작업 현황판** | `app/core/jobs.py` · `GET /api/admin/jobs` | 2026-08-16. 실행 중 1건 + 최근 5건 + 다음 단계 |
| **답변 피드백 👍/👎** | `app/core/feedback.py` · `POST /api/feedback` | 2026-08-17. 질문 이력 `평가` 열 · 검수 `사용자 신고`·`👎 많은순` |
| **인트로 주제 펼침** | `app/static/chat.*` | 2026-08-17. `전체 주제 보기` 가 그 자리에서 열립니다 |
| **문서 그림(도식)** | `GET /api/docs/img/{파일}` · `chat.js` 의 `renderDoc()` | 2026-08-17. 출처 모달이 제목·표·그림을 렌더. 시연: `가입했는데 메뉴가 안 보입니다` |
| **QA 가져오기** | `app/qa/importer.py` · `POST /api/admin/qa/import[/preview]` · `scripts/push_qa.py` | 2026-08-18. 스튜디오 파일을 서버로. **무조건 검수 대기**로 들어옴 |

### 안 되는 것 ❌ (= 남은 일)

| # | 기능 | 왜 필요한가 |
| --- | --- | --- |
| ⑤ | 배포 — **개발 서버 반입** | 이미지는 만들어 띄워 봤습니다(2026-08-17). 서버에 올려 본 적은 없습니다. AKS 매니페스트도 아직 없습니다 |
| — | 실사용 검증 | 화면·API는 붙었지만 **사람이 QA를 쌓아 본 적이 없습니다** |
| — | "답이 없어야 하는 질문" 평가 | 지금 탭 ⑦은 승인된 QA의 변형 질문만 문항으로 씁니다 — 전부 *답이 있어야 하는* 질문이라, **답하지 말아야 할 때 답하는** 경우를 못 잽니다. 1차의 `is_confusion_test`(`forever-chat/app/eval/question_gen.py`)가 그 자리입니다 |
| — | 문서 그림 — **화면 데모까지 됨**(2026-08-17) | 답변은 텍스트로 두고 **원본 문서에만** 그림을 넣습니다. 답변마다 박으면 화면이 바뀔 때 갱신 지점이 수십 곳으로 늘어납니다. 출처 모달이 마크다운(제목·표·그림)을 렌더하고 `GET /api/docs/img/{파일}` 이 그림을 줍니다. 남은 것은 **실제 화면 캡처를 넣는 일**입니다 |

**지금 필요한 것은 코드가 아니라 QA입니다.** 생성(탭 ⑥)해서 승인(탭 ④)한 만큼만 챗봇이
답합니다. 임계값 근거(탭 ⑦)도 QA가 어느 정도 쌓인 뒤라야 의미 있는 숫자가 나옵니다.

---

## 3. 반드시 알아야 할 두 가지 함정

### 3-1. `data/chroma/` 는 저장소에 없습니다 (의도한 것)

벡터 저장소는 **파생물**입니다. 원본은 이 셋입니다.

```
data/qa_index.json      ← 사용자에게 나가는 답변 전부. 가장 중요한 파일
data/categories.json    질문 주제
data/raw_docs/*.md      원본 문서
```

git clone 직후엔 벡터가 없어 **모든 질문이 `unresolved`** 로 떨어집니다. 버그가 아닙니다.
`python scripts/seed_demo_qa.py --force` 또는 아래로 만듭니다.

```bash
curl -u admin:비밀번호 -X POST "http://localhost:18100/api/admin/qa/reindex?include_docs=true"
```

이 구조를 택한 이유: 폐쇄망 배포가 **파일 3개 복사**로 끝납니다. DB를 세울 필요가 없고,
검수 결과를 사람이 읽고 diff로 볼 수 있습니다. 벡터는 그게 안 됩니다.

### 3-2. 퍼블 산출물은 **어느 프로젝트 것인지 먼저 확인**하세요

2026-08-15 에 `퍼블/` 을 올바른 산출물(관리자 8탭 + 로그인 모달)로 교체했습니다. 그전까지는
**이전 프로젝트(`openapi-chat`) 것**이 같은 이름으로 들어 있었고, 실제로 한 번 헷갈렸습니다.
받은 산출물을 열면 30초 안에 판별할 수 있습니다.

| 확인할 곳 | 이번 프로젝트 | 이전 프로젝트 |
| --- | --- | --- |
| `admin-mapping.md` 첫 줄 | `admin 화면 매핑 문서` | `openapi-chat 관리자 화면` |
| 탭 | `history/analytics/categories/qa/docs/generate/eval/settings` (8개) | `prompt/docs/categories/analytics/thresholds` (5개) |
| `chat.js` 필드명 | `result_type` · `group_name` · `category_id` | `answered_by` · `group_label` · `id` |

**이식이 끝난 정본은 `app/static/` 입니다.** `퍼블/` 은 원본 보관용이라 직접 서비스되지 않습니다.

---

## 4. 구조

```
app/
  api/
    ask.py              POST /api/ask · /api/support · GET /api/categories · /api/docs/*
    admin_qa.py         QA 목록·저장·일괄작업·재색인
    admin_docs.py       문서 CRUD (serve 모드는 읽기 전용)
    admin_questions.py  질문 이력 + CSV
    admin_settings.py   임계값 · 카테고리 · 모드 정보
    admin_auth.py       로그인 · 로그아웃 · 세션 확인 (이 라우터만 인증 없음)
    admin_analytics.py  질문 분석 실행 · 결과 · 묶음 상태 지정 (탭 ②)
    studio_generate.py  QA 생성 시작/진행률/중지/결과/반영 + 변형 질문 (studio 전용, serve 는 403)
    studio_eval.py      품질 평가 실행/진행률/결과 (studio 전용)
    health.py           /health · /health/ready
  core/
    config.py           환경변수 (APP_MODE, 임계값, 경로, 세션)
    auth.py             세션 쿠키 또는 Basic (compare_digest, 바이트 비교)
    session.py          서명된 세션 토큰 (저장소 없음 — HMAC 서명만 확인)
    runtime_config.py   운영 중 바뀌는 설정 (관리자 탭 ⑧)
    categories.py       카테고리 저장소
    question_log.py     질문 이력 + 임베딩 사이드카
    jsonstore.py        원자적 JSON 쓰기 (임시파일 → os.replace)
  ingestion/
    chunker.py          마크다운 → 청크 (## 와 ### 둘 다 분할)
    embedder.py         임베딩 + 모델별 태스크 접두어
    vector_store.py     Chroma 클라이언트 · 컬렉션 2개
    doc_index.py        문서 색인/검색 (related_docs 용)
  qa/
    store.py            QA 저장소 (qa_index.json)
    index.py            QA 벡터 색인/검색
  pipeline/
    retrieve.py         ★ 질문 하나가 지나가는 전체 경로. 여기부터 읽으세요
    analytics.py        질문 군집 + 묶음 상태 (LLM 없음 — 운영에서도 동작)
  studio/               ★ 이 프로젝트에서 LLM을 부르는 유일한 곳 (studio 모드 전용)
    llm.py              Ollama chat 한 겹 — 빈 응답 예외 + 컨텍스트 초과 사전 절단
    korean.py           is_korean() — 다른 언어가 섞인 결과를 걸러낸다
    generate.py         문서/질문 → QA 초안(질문·답변·변형 질문)
    runner.py           백그라운드 실행 + 진행률 + 중지 + 초안 파일 + pending 반영
    evaluate.py         검색 품질 평가 (변형 질문을 문항으로, 자기 자신은 제외)
  static/               퍼블 산출물 이식본 + jQuery(vendor)
    chat.*              챗봇 화면 (퍼블 산출물 이식본 — 다시 오면 통째로 교체)
    admin.*             관리자 8개 탭 (이식본. 머리 부분과 저장 지점만 우리가 씀)
    review.*            QA 검수 임시 화면 (serve 모드에서 승인할 때. 5-13)
scripts/
  seed_categories.py    퍼블 더미 → categories.json (1회용)
  seed_demo_qa.py       문서 색인 + 데모 QA 3건
  generate_qa.py        QA 생성 배치 (화면 없이 돌릴 때)
  launch.py / stop.py   실행.bat / stop.bat 이 부르는 런처
tests/                  94개. conftest.py 가 임시 디렉터리 + 가짜 임베딩/가짜 LLM으로 격리
docs/퍼블요청/           퍼블리셔에게 보낸 요청서 3건 + 받은 매핑 문서
```

**코드를 처음 볼 때 읽는 순서**: `app/pipeline/retrieve.py` → `app/qa/store.py` → `app/qa/index.py`

---

## 5. 설계 결정과 이유 (고치기 전에 확인)

각 항목은 실제로 문제를 겪고 내린 판단입니다. 되돌리려면 같은 문제가 다시 옵니다.

### 5-1. 승인(`approved`)된 QA만 벡터 색인에 올린다

`pending`/`hold`/`disabled` 는 파일에는 남지만 검색되지 않습니다.
AI 초안이 검수 없이 사용자에게 새어 나가는 경로를 **구조적으로** 없앤 것입니다.
→ `app/qa/store.py` 의 `SERVING_STATUSES`

### 5-2. 변형 질문은 항상 "먼저 지우고 다시 넣는다"

upsert만 하면 삭제한 변형 질문의 벡터가 남아 계속 걸립니다.
검수자가 고쳤는데 동작이 안 바뀌는, 알아채기 어려운 버그입니다.
→ `app/qa/index.py` 의 `upsert_item`, 테스트 `test_deleted_variant_stops_matching`

### 5-3. 임베딩은 질문당 한 번만 계산해 세 곳에서 돌려 쓴다

QA 검색 / 문서 검색 / 질문 이력. 가장 비싼 단계라 두 번 돌리면 응답 시간이 두 배입니다.
이력에 남긴 벡터는 나중에 질문 분석이 재사용합니다 — GPU 없는 운영에서 재임베딩은 수십 분입니다.
→ `app/pipeline/retrieve.py`, `app/core/question_log.py` 하단

### 5-4. 청킹을 `##` 뿐 아니라 `###` 까지 내렸다

이전엔 청크가 3천 자까지 커져 (a) 벡터가 뭉개지고 (b) 발췌 2~3줄을 어디서 잘라야 할지
알 수 없었습니다. 대신 `###` 는 부모 제목을 잃으므로 `section_path` 에 `부모 > 자식` 으로 남깁니다.
→ `app/ingestion/chunker.py`

### 5-5. 카테고리 필드명을 퍼블 산출물에 맞췄다

`group_id` / `group_name` / `category_id` / `name` / `questions`.
서버가 다른 이름을 쓰면 변환 계층이 생기고, 그 계층은 화면을 고칠 때마다 틀어집니다.
→ `app/core/categories.py`

### 5-6. 임계값을 화면이 아니라 서버가 판단한다

`result_type` 은 서버가 정하고 화면은 그대로 그립니다. 화면이 유사도를 보고 다시 판단하면
관리자 설정과 실제 동작이 조용히 어긋납니다.
→ `app/static/chat.js` 의 `render()`

### 5-7. `related_docs_floor < qa_match_threshold` 를 서버가 강제한다

역전되면 `related_docs` 구간이 사라져 답을 못 찾은 질문이 전부 `unresolved` 로 떨어집니다.
화면도 막지만 API 직접 호출 경로가 있어 서버에서도 세웁니다.
→ `app/core/runtime_config.py` 의 `_floor_below_match`

### 5-8. 접수번호(ticket_id)는 서버가 만든다

화면에서 만들면 사용자가 본 번호와 이력에 남은 번호가 달라져, 담당자가 번호로 찾을 수 없습니다.
→ `POST /api/support`

### 5-9. 생성 결과는 `pending` 으로만 들어간다 (2026-08-15)

`apply` 에 상태 인자를 두지 않았습니다. **생성 단계에서 approved 로 넣는 경로를 아예 만들지
않은 것**이 5-1을 실제로 보장하는 방법입니다. 벡터 색인도 부르지 않습니다 — `pending` 은 색인
대상이 아니라 넣을 것이 없고, 여기서 색인을 부르면 "생성 직후 사용자에게 나갈 수 있다"는
오해를 코드가 만들어 줍니다.
→ `app/studio/runner.py` 의 `apply_drafts`, 테스트 `test_apply_saves_as_pending_and_stays_out_of_index`

### 5-10. 근거를 못 찾은 질문은 답을 만들지 않고 버린다 (2026-08-15)

질문에서 시작하는 생성(`generate_from_questions`)은 모델에게 `근거: 있음|없음` 을 **스스로
선언**하게 하고, `없음` 이면 그 질문을 버립니다. 1차 프로젝트 3-3에서 유사도 숫자만 보고
판단했다가 "점심 메뉴 추천"에 억지 답변이 나간 적이 있습니다.

버려진 질문은 "문서가 없다"는 신호입니다. 지어낸 답변으로 그 신호를 덮으면 문서 공백이 영영
드러나지 않고, 검수자는 대조할 원문이 없는 답변을 승인하게 됩니다.
→ `app/studio/generate.py` 의 `_parse_grounded_answer`

### 5-11. 변형 질문은 LLM을 한 번 더 불러서 만든다 (2026-08-15)

질문·답변과 한 번에 만들게 하면 모델이 형식을 지키느라 변형을 2~3개로 줄입니다. 호출을 나누면
항목당 7~10개가 안정적으로 나옵니다(실측). 적중률은 사실상 변형 질문 목록이 결정하므로
호출 수보다 이쪽이 중요합니다. 어차피 스튜디오에서 도는 배치입니다.

### 5-13. 검수 화면은 임시로 따로 만들었다 (2026-08-15)

퍼블 산출물을 기다리는 동안 승인 경로가 막혀 있었습니다. 생성한 답변은 **승인해야 나가므로**,
검수 화면이 없으면 앞 단계가 전부 무의미해집니다.

- **2026-08-16 에 걷어냈습니다.** 관리자 화면에 `검수` 항목이 생겼고, 편집 폼을 두 벌로 두면
  한쪽만 고쳐지기 때문입니다. 아래는 그때의 기록입니다
- 주소는 `/admin/review`, 파일은 `app/static/review.*`, 클래스 접두어는 `rev_` 였습니다
- API는 **새로 만들지 않았습니다.** `/api/admin/qa` 가 원래 탭 ④(QA 인덱스)용으로 만든
  컨트롤러이고, 이 화면이 그 탭을 임시로 대신하는 것이라 같은 화면으로 봅니다
- 답변 미리보기는 `chat.js` 의 `renderMarkdown` 을 **복사**했습니다. 공용 파일로 빼지 않은
  이유는 `chat.js` 가 퍼블 산출물 이식본이라 통째로 갈아 끼우기 때문입니다.
  **`chat.js` 의 렌더 규칙을 고치면 `admin.js` 도 같이 고치세요** — 검수자가 보는 모습과
  사용자가 보는 말풍선이 달라지면 검수가 의미를 잃습니다

`/admin` 은 `admin.html` 이 없을 때 500 대신 안내 페이지를 냅니다.

### 5-12. 문서 도입부에서는 QA를 만들지 않는다 (2026-08-15)

도입부(첫 헤딩 앞)는 "이 문서는 ~를 설명합니다"라서, 거기서 만든 답변은 업무가 아니라 **문서
자체를 설명**합니다. `gemma4` 첫 시험 생성에서 실제로 그런 항목이 1번으로 나왔습니다.
검색(`related_docs`)에는 도입부도 쓸모가 있으므로 청킹이 아니라 생성 단계에서만 뺍니다.
헤딩이 없는 문서는 전부 도입부처럼 보이므로 그때는 빼지 않습니다.
→ `app/studio/generate.py` 의 `_is_document_lead`

### 5-15. 인증은 세션 쿠키 + Basic **둘 다** 받는다 (2026-08-15)

로그인 모달이 생기면서 세션 쿠키를 도입했지만, Basic 인증을 걷어내지 않았습니다.
배포·점검 절차가 `curl -u admin:비밀번호 ... /reindex` 로 문서에 적혀 있어서, 한쪽을 없애면
문서와 손이 전부 어긋납니다. → `app/core/auth.py` 의 `require_admin`

**세션 저장소는 두지 않습니다.** 토큰 안에 사용자와 만료 시각을 적고 HMAC 으로 서명해,
서버는 서명만 확인합니다. 저장소를 두면 배포 대상 파일이 하나 늘고 파드가 여러 개면 공유
저장소까지 필요해집니다. 서명 키는 기본적으로 기동 때마다 새로 만듭니다(재시작하면 로그인이
풀리지만, 키를 파일에 남기지 않는 편이 안전합니다). → `app/core/session.py`

**401에 `WWW-Authenticate` 를 붙이지 않습니다.** 붙이면 브라우저 기본 로그인 창이 우리 모달과
겹쳐 뜨고, 그 창으로 로그인하면 화면은 로그인 사실을 모릅니다. `curl -u` 는 처음부터 헤더를
붙여 보내므로 이 헤더 없이도 동작합니다.

관리자 HTML(`/admin`)은 **인증 없이 내려갑니다.** 로그인 모달이 그 페이지 안에
있어서 페이지를 막으면 로그인할 화면도 막힙니다. 데이터는 전부 인증 걸린 API에서 옵니다.

> 이때 `compare_digest` 가 **비ASCII 문자열에서 예외를 낸다**는 것도 함께 고쳤습니다.
> 로그인 칸에 한글을 치면 401이 아니라 500이 났습니다(한영 전환 실수는 흔합니다).

### 5-16. 평가 문항은 검수된 QA의 변형 질문을 쓴다 (2026-08-15)

골든셋 파일을 따로 만들지 않았습니다. 변형 질문은 "이 표현이 오면 이 답" 을 사람이 확인해 둔
것이라 정답 라벨이 이미 붙어 있습니다. 파일을 따로 두면 QA를 고칠 때마다 같이 관리해야 합니다.

**단, 문항이 인덱스에 이미 들어 있습니다.** 그대로 검색하면 자기 자신이 유사도 1.000으로
1등이 되어 적중률이 항상 100%로 나옵니다. 그래서 검색 결과에서 **글자가 같은 항목을 빼고**
순위를 매깁니다. 벡터를 실제로 뺐다 넣는 방법도 있지만 느리고, 도중에 실패하면 인덱스가
깨집니다. → `app/studio/evaluate.py`

결과에는 임계값별 표(0.75~0.95)가 함께 옵니다. **오매칭률을 먼저 보세요** — 못 찾으면 사용자가
다시 묻지만, 엉뚱한 답이 나가면 그것이 맞는 줄 압니다.

### 5-19. 역할마다 다른 모델을 쓴다 (2026-08-16)

같은 문서(`포털-전체개요`)·같은 설정으로 두 모델을 돌려 나란히 놓고 정했습니다.

| | `gemma4:latest` | `gemma4:12b` |
| --- | --- | --- |
| 항목당 시간 | 17초 | 76초 |
| 답변 | 사용자 역할 하나만 설명(54자) | 셋을 모두 서술(284자) |
| 변형 질문 | 구어체·명사 나열까지 폭이 넓음 | 원문 어순만 바꿔 폭이 좁음 |

**강점이 반대였습니다.** 적중률은 사실상 변형 질문의 표현 폭이 정하므로 질문·변형에는 작고
빠른 모델, 답변에는 큰 모델을 둡니다(`OLLAMA_QUESTION_MODEL` / `OLLAMA_ANSWER_MODEL`).

주제는 모델이 아니라 **청크가 정합니다.** 두 모델이 뽑은 주제가 정확히 같았습니다(전체 절차 /
배포 신청 조건 / 사용자 역할). 질문 가짓수를 늘리려면 모델이 아니라 관점이나 청킹을 손대야 합니다.

이때 문서 경로를 **질문 호출 + 답변 호출**로 쪼갰습니다. 호출이 늘지만 얻은 것이 둘입니다 —
역할별로 모델을 나눌 수 있게 됐고, 문서 경로에도 `근거: 있음|없음` 판정이 걸렸습니다
(전에는 질문 경로에만 있었습니다).

### 5-20. 채점 모델은 답변 모델과 계열이 달라야 한다 (2026-08-16)

1차의 `llm_judge.py` 를 옮기면서 **채점 대상을 바꿨습니다.** 1차는 즉석 생성 답변을 골든셋의
기대 답변과 비교했지만, 여기에는 골든셋이 없고 대신 **근거 발췌**가 있습니다. 그래서 묻는 것이
"기대 답변과 같은가"가 아니라 **"발췌에 있는 사실만 말하는가"** 입니다.

정한 것 셋:

- **비워 두면 채점하지 않습니다.** 답변 모델로 자기 답을 채점하면 점수가 후해지고, 문서를
  잘못 읽었다면 채점할 때도 같은 오독을 반복합니다. 있으나 마나 한 점수가 붙는 것보다 없는
  편이 낫습니다 — 숫자가 있으면 검수자가 그것을 믿습니다
- **0점은 '나쁨'이 아니라 '판정하지 않음'입니다.** 1차는 JSON 파싱이 실패하면 1점(최악)을
  줬는데, 그러면 모델이 형식을 흘린 것만으로 멀쩡한 초안이 버려집니다. 지금은 초안 목록에
  남고 자동 반영에서만 빠집니다
- **채점이 승인을 대신하지 않습니다.** 5점이어도 `pending` 이고 사람이 승인해야 나갑니다.
  자동 필터는 검수량을 줄이는 장치이지 검수를 없애는 장치가 아닙니다

지금 판정 모델은 `exaone3.5:7.8b`(LG AI) 입니다. 답변 모델이 `gemma4:12b` 라 계열이 다릅니다.
단독 시험에서 정확한 답 5점 / 발췌에 없는 내용 1점 / 질문과 무관 1점으로 갈렸습니다.

### 5-21. 폴더 업로드는 화면이 나눠 보낸다 (2026-08-16)

파일 이름이 곧 문서 ID입니다(`api-등록.md` → `api-등록`). 사람이 ID를 다시 적지 않으니 오타로
같은 문서가 두 벌 생기지 않고, 폐쇄망에 `data/raw_docs/` 를 복사해 넣는 배포 방식과 결과가
같아집니다.

**한 요청에 폴더 전체를 받지 않습니다.** 문서 한 건마다 청킹·임베딩이 돌아 몇 초씩 걸립니다.
40건을 한 번에 받으면 진행 상황을 보여줄 수 없고, 프록시 타임아웃에 걸린 뒤 무엇이 들어갔는지
알 수 없게 됩니다. 화면이 4건씩 나눠 보내고 서버는 그 묶음만 처리합니다(상태 없음).

걸러내는 것: 문서가 아닌 파일 · 하위 폴더가 달라도 이름이 같은 파일(먼저 온 것만) · 앞머리
YAML이 깨진 파일(**파일을 쓰기 전에** 걸러야 "목록엔 있는데 청크 0"인 문서가 안 생깁니다).
CP949·CRLF·macOS 자모분리(NFD) 이름은 정상 처리하되, 인코딩을 판별할 수 없으면 거부합니다 —
깨진 채 색인되면 예외 없이 검색 품질만 조용히 무너집니다.

### 5-22. 작업 현황판 — 다음 단계는 서버가 정한다 (2026-08-16)

수십 분짜리 배치인데 그 탭에 머물러야만 진행이 보였고, 새로고침하면 사라졌습니다.
`GET /api/admin/jobs` 하나로 모았습니다.

- **실행 중은 잡에서 직접 읽습니다.** 생성·평가는 각자 상태를 들고 있어서, 이력에 복사해 두면
  두 값이 어긋납니다. 이력(`app/core/jobs.py`)은 **끝난 것만** 적습니다
- **색인·업로드는 이력만 남습니다.** 요청 안에서 동기로 끝나 '몇 %'를 물어볼 대상이 없습니다
- **`next`(다음에 할 일)는 서버가 정합니다.** 화면이 작업 종류를 보고 추측하면 순서를 바꿀
  때마다 둘이 다른 것을 안내합니다 — `result_type` 을 서버가 정하는 것과 같은 원칙입니다
- **업로드 묶음은 한 줄로 합칩니다.** 요청마다 남기면 폴더 하나가 열 줄을 차지합니다.
  요약은 마지막 묶음이 아니라 **합계로 다시 계산**합니다

> 이력 요약을 만들다 필드 이름을 틀렸는데 그 예외가 생성 배치를 통째로 '실패'로 뒤집은 적이
> 있습니다. `jobs.record()` 는 이제 자기 예외를 밖으로 내보내지 않습니다 — 부가 정보가 본
> 작업을 죽이면 안 됩니다.

### 5-23. 화면의 입력칸은 전부 서버로 간다 (2026-08-16)

퍼블 산출물에 값이 안 먹는 입력칸이 셋 있었습니다 — `답 없는 질문 비율`(1차의 평가셋 설정),
역할 분리 전의 `생성 모델`, LLM을 쓰지도 않는 평가의 `판정 모델`.

**값이 안 먹는 입력칸은 오류보다 나쁩니다.** 오류는 눈에 띄지만 이건 검수자가 값을 조절하며
결과가 달라지기를 계속 기다립니다. `.env` 에서 오는 값(모델·컨텍스트 창·최대 길이)은 입력칸이
아니라 **읽기 전용 표시**로 두고, 어디서 오는지 한 줄 적었습니다. 규칙은
`tests/test_admin_screen.py` 가 세 화면(생성·품질 평가·생성 설정)에서 지킵니다.

### 5-24. 채점은 JSON 강제 + 추론 끄기로 부른다 (2026-08-17)

`qwen3:4b` 로 채점하다가 **영어 사고 과정을 4,071자 뱉고 JSON 에 도달하지 못해** 판정이
실패한 일이 있습니다. 출력 예산(`num_predict`)을 사고 과정이 다 써버린 것입니다. 그 결과
**멀쩡한 답변(4점짜리)이 '판정 실패'로 걸러져** 반영에서 빠졌습니다.

`OLLAMA_THINK=false` 로는 막히지 않았습니다. 둘을 함께 걸어야 합니다.

```python
llm.chat(prompt, json_format=True)        # Ollama 에게 JSON 만 내보내게
"/no_think\n" + prompt                    # Qwen3 계열 프롬프트 스위치 (llm.py 가 자동으로 붙임)
```

고친 뒤 같은 건이 **4점 · 3초**로 처리됩니다(전에는 실패에 1분 이상). 게다가 판정 품질이
가장 좋았습니다 — 발췌에 없는 경로를 유일하게 잡아냈고, `qwen2.5-coder`·`gemma4:e2b` 는
둘 다 5점을 줬습니다.

> 1차의 "추론 모델 빈 응답"과 같은 함정입니다. **추론 모델을 쓸 때는 형식이 깨지는 쪽을
> 먼저 의심하세요.** 모델 이름으로 판단해 `/no_think` 를 붙입니다(`_NO_THINK_MODELS`).

### 5-25. 역할별 모델은 **단계별로 묶어서** 돌려야 한다 (2026-08-17, 미구현)

지금 생성은 **항목 단위**로 파이프라인을 돕니다.

```
항목1(질문 A → 답변 B → 변형 A → 채점 C) → 항목2(…) → …
```

세 모델을 합치면 GPU에 다 올릴 수 없어 **항목마다 3~4번 모델을 다시 로드**합니다.
15건이면 로드만 45번이고, 실측에서 항목당 몇 분씩 걸렸습니다(한 모델일 때는 17초).

**단계 단위로 바꾸면 로드가 4번으로 끝납니다.**

```
모든 질문(A) → 모든 답변(B) → 모든 변형(A) → 모든 채점(C)
```

정하고 넘어갈 것: 중지했을 때 "답변만 있고 채점은 없는" 중간 상태를 어떻게 다룰지.
지금은 항목이 완성돼야 초안이 되므로 중지해도 상태가 깔끔합니다.

### 5-18. 퍼블 산출물이 다시 오면 **추가분만** 옮긴다 (2026-08-15)

챗봇 보완 산출물(요청서 04 — 새 대화·답변 복사·맨 아래로)이 왔을 때, 파일을 통째로 갈아
끼우지 않고 **바뀐 블록만** 옮겼습니다. 이식본에는 서버 배선(`/api/ask` 호출 등)이 들어 있어
덮어쓰면 그 작업을 처음부터 다시 해야 합니다.

방법: 저장소의 `퍼블/`(직전 산출물) ↔ 새로 받은 산출물을 diff 해서 **추가된 블록만** 뽑아
이식본에 붙입니다. 그래서 `퍼블/` 에 **직전 산출물을 그대로 보관**하는 것이 중요합니다.
비워 두면 무엇이 새로 온 것인지 구분할 방법이 없습니다.

> 이때 산출물의 스크롤 로직에서 버그를 하나 찾아 고쳤습니다. 말풍선을 **붙인 뒤에** 하단
> 여부를 재고 있었는데, 그러면 답변이 160px보다 길 때 방금 붙인 말풍선 높이 때문에
> '하단 아님'으로 판정되어 **하단에서 기다리던 사용자에게 자동 스크롤이 안 됩니다.**
> 붙이기 전에 재도록 바꿨습니다(`app/static/chat.js` 의 `pushed`).

### 5-17. 관리자 화면은 "머리 부분과 저장 지점"만 우리가 쓴다 (2026-08-15)

퍼블 산출물(`admin.js` 1,678줄)에서 **렌더·이벤트 코드는 그대로 두고**, 최상단 더미 데이터를
서버 호출로 바꾸고 저장 지점(`mockSave`/`setTimeout` 흉내)만 실제 API로 교체했습니다.

화면 로직에 서버 사정을 섞기 시작하면 다음 산출물이 올 때마다 같은 배선을 처음부터 다시 하게
됩니다. 서버 응답을 화면 모양으로 바꾸는 변환도 **한 곳(파일 머리)** 에만 뒀습니다
(`approved→done`, `enabled→used`, `chunk_count→chunks` 등).

> 화면이 부르는 `/api/...` 경로가 서버에 실제로 있는지 테스트로 고정했습니다
> (`tests/test_admin_screen.py`). 경로를 한 글자 틀리면 탭 하나가 조용히 비어 보입니다.

### 5-14. `.bat` 에는 한글을 쓰지 않는다 — 주석에도 (2026-08-15)

더블클릭 런처를 처음에는 배치 파일 하나로 썼는데 **한글이 섞이자 파서가 깨졌습니다.** cmd 는
배치 파일을 콘솔 코드페이지(한국어 Windows 는 949)로 읽는데 파일은 UTF-8이라, 한글 바이트에서
바이트 오프셋이 어긋나며 줄 중간이 잘려 나갔습니다. 단어 조각이 명령으로 실행돼
`'ps' is not recognized` 같은 오류가 나고, 라벨을 건너뛰어 엉뚱한 곳부터 실행됐습니다.
`chcp 65001` 을 첫 줄에 넣어도 같았고, **`rem` 주석 안의 한글도 똑같이 깨졌습니다.**

그래서 `.bat` 은 **ASCII만** 두고(파이썬을 찾아 스크립트를 부르는 몇 줄), 판단과 안내 문구는
`scripts/launch.py` · `scripts/stop.py` 로 옮겼습니다. 파이썬은 콘솔에 유니코드로 직접 쓰므로
코드페이지와 무관합니다. 파일 **이름**이 한글인 것(`실행.bat`)은 문제가 없습니다.

두 스크립트는 출력 인코딩도 UTF-8로 고정합니다. 출력을 파일이나 파이프로 넘기면 파이썬이
cp949 로 인코딩하는데, 거기 없는 글자(예: `—`)가 하나만 있어도 `UnicodeEncodeError` 로
**런처 자체가 죽습니다.** 안내 문구 때문에 실행이 막히는 일은 없어야 합니다.

---

## 6. 다음에 할 일 (상세)

### ① QA 사전 생성 — **완료 (2026-08-15)**

문서·질문에서 QA 초안 + 변형 질문을 만들어 `pending` 으로 넣는 기능이 들어갔습니다.

```
app/studio/{llm,korean,generate,runner}.py   생성 로직
app/api/studio_generate.py                   시작/진행률/중지/결과/반영
scripts/generate_qa.py                       배치 실행 (화면이 없는 지금의 유일한 수단)
```

```bash
# .env 에 APP_MODE=studio
python scripts/generate_qa.py --max 3 --variants 12   # 소량으로 품질 먼저 확인
python scripts/generate_qa.py --apply                 # 초안을 pending 으로 반영
```

화면(탭 ⑥)에서도 같은 API를 부릅니다 — 대상 선택 → 시작 → 진행률 폴링 → 결과 체크 → 반영.

실제 `gemma4:latest` 로 확인한 값(2026-08-15, 로컬 PC):

| 항목 | 값 |
| --- | --- |
| 한 항목 생성 시간 | 약 1분 (본문 1회 + 변형 1회) |
| 항목당 변형 질문 | 8개 요청 → 7개 (중복·대표질문과 동일한 것 제외) |
| 답변 길이 | 2~3문장. 규칙은 3~6문장인데 짧게 나오는 편입니다 |
| `category_id` | 대부분 `null` — 문서 frontmatter 의 분류와 카테고리 이름이 정확히 겹칠 때만 채웁니다 |

주의:
- 첫 시험 생성에서 **문서 도입부에서 메타 질문**("이 문서는 무엇을 설명하나요")이 나왔고,
  답변에 문서 내부 표기(`[[api-등록]]`)가 새어 나왔습니다. 둘 다 막아 뒀습니다(5-12, `clean_answer`)
- 앞으로도 프롬프트를 고치면 **소량(`--max 2`)으로 먼저 눈으로 보세요.** 형식 이탈은
  테스트로 안 잡힙니다(테스트의 LLM은 가짜입니다)

### ② 품질 평가 — **완료 (2026-08-15)** · studio 전용

`app/studio/evaluate.py` · `POST /api/studio/eval` · 화면 탭 ⑦.

검수된 QA의 **변형 질문을 그대로 문항으로** 씁니다(골든셋 파일 없음). 문항이 인덱스에 이미
들어 있으므로 **글자가 같은 항목을 빼고** 순위를 매깁니다 — 안 그러면 적중률이 항상 100%입니다.
자세한 판단은 5-16.

측정: 적중률(Top-1/Top-3) · **오매칭률** · 미검색 + **임계값별 표**(0.75~0.95).
임계값을 정할 때는 오매칭률을 먼저 보세요.

> **아직 숫자를 못 냈습니다.** 승인된 QA가 적어서입니다. QA를 쌓은 뒤 돌려 보세요.
> 임베딩 모델 비교(`bge-m3` vs `embeddinggemma`)도 이 표로 하면 됩니다 —
> 벤치마크 순위는 믿지 마세요. 이전에 MTEB 순위가 우리 문서에서 뒤집혔습니다.

### ③ 관리자 화면 — **완료 (2026-08-15)**

퍼블 산출물(8개 탭 + 로그인 모달)이 도착해 `app/static/admin.*` 로 이식하고 API에 연결했습니다.

| 탭 | API |
| --- | --- |
| ① 질문 이력 | `GET /api/admin/questions` `/export` |
| ② 질문 분석 | `GET/POST /api/admin/analytics` `/run` `/override` |
| ③ 카테고리 | `GET/PUT /api/admin/categories` |
| ④ QA 인덱스 | `GET/POST /api/admin/qa` `/{id}` `/bulk` `/reindex` |
| ⑤ RAG 문서 | `GET/POST/PUT/DELETE /api/admin/docs` |
| ⑥ QA 생성 | `POST /api/studio/generate` `/progress` `/stop` `/result` `/apply` `/variants` (studio) |
| ⑦ 품질 평가 | `POST /api/studio/eval` `/progress` `/result` (studio) |
| ⑧ 설정 | `GET/PUT /api/admin/settings` `/reset` |
| 로그인 | `POST /api/admin/login` `/logout` · `GET /api/admin/session` |

`/admin` 은 `<body data-mode="serve">` 를 **서버가 직접 치환**해 내려줍니다(`app/main.py`).
화면이 켜진 뒤 API로 물어보면 studio 전용 탭이 잠깐 보였다 사라집니다.

**주의: serve 모드에서는 `QA 인덱스` 의 편집이 읽기 전용입니다**(퍼블 요청서 규약 — 검수는
스튜디오에서 하고 결과 파일을 배포한다는 전제). 다만 **`검수` 화면의 승인은 serve 에서도
됩니다** — 운영에 쌓인 질문에서 만든 QA를 그 자리에서 승인해야 하는 경우가 있습니다.

이식 방법과 다시 이식할 때 볼 것은 5-17에 적었습니다.

### ④ 질문 분석/군집 — **완료 (2026-08-15)**

`app/pipeline/analytics.py` · `POST /api/admin/analytics/run` · 화면 탭 ②.

**LLM을 쓰지 않습니다** — 대표 질문은 군집 중심에 가장 가까운 실제 질문이고, 요약은 묶인 표현
가짓수를 셉니다. 그래서 운영(serve)에서도 돌아갑니다. 질문 임베딩은 답변할 때 저장해 둔 것을
재사용해 재임베딩이 없습니다(GPU 없는 운영에서는 이게 필수입니다).

사람이 지정한 상태·주제는 `overrides` 로 따로 보관해 **다시 분석해도 남습니다.**

### ⑤ 배포 — **이미지까지 만들어 확인했습니다 (2026-08-17)**

`Dockerfile` · `.dockerignore` · `docker-compose.yml` 을 저장소 루트에 넣었고, **개발 PC 에서
빌드해 띄우고 질문까지 던져 봤습니다.** 남은 것은 개발 서버 반입입니다. 절차와 측정값은
[docs/11-배포.md](docs/11-배포.md).

| | 값 |
| --- | --- |
| 이미지 | 2.66GB (모델 565MB + `onnxruntime`·`chromadb` 의존성) |
| `docker save` | **902MB** — 옮기는 건 이만큼. `gzip` 은 효과 없음(이미 압축된 레이어) |
| 첫 질문 | 9.7초 (모델 적재) |
| 이후 질문 | 31~55ms — 컨테이너 밖과 같음 |

**도커가 선택이 아니라 필수인 이유**: 개발 서버가 CentOS 7(glibc 2.17)인데 `onnxruntime`
휠은 glibc 2.28 을 요구합니다(2026-08-17 PyPI 확인). 호스트에 직접 설치할 수 없습니다.

```
onnxruntime 1.28.0  →  manylinux_2_28   ✗
tokenizers · numpy  →  manylinux2014    ✓
chromadb            →  py3-none-any     ✓
```

배포할 때 챙길 것:

- 운영은 `APP_MODE=serve` — studio 전용 라우터는 403을 냅니다(탭도 숨겨집니다)
- **리눅스 서버에서는 `sudo chown -R 10001:10001 data/` 를 먼저 합니다.** 컨테이너가
  비루트(uid 10001)로 돕니다. 윈도우 bind mount 는 `rwxrwxrwx` 로 보여 그냥 됐지만 리눅스는
  호스트 권한이 그대로 넘어갑니다. 안 맞추면 **조회는 되는데 승인·설정 저장이 조용히 실패**합니다
- **`data/` 를 이미지에 굽지 않았습니다.** 검수 결과와 질문 이력이 쌓이는 곳이라 볼륨입니다.
  대신 명명 볼륨을 쓰게 되면 이전 프로젝트의 **시드 디렉터리 패턴**이 필요합니다(3-10)
- `ADMIN_PASSWORD` 는 반드시 바꿉니다. 기본값이면 기동 로그에 경고가 남습니다
- `SESSION_SECRET` 은 파드가 하나인 지금은 없어도 되지만, 늘리는 순간 필수입니다
- 옮길 파일은 여전히 셋입니다 — `qa_index.json` · `categories.json` · `raw_docs/`
- 개발 서버는 인터넷에 노출돼 있습니다(7번). `docker-compose.yml` 이 포트를
  **`127.0.0.1:18100`** 에만 묶어 뒀고, 접속은 SSH 터널로 합니다

---

## 7. 환경 정보

### 개발 PC (현재)

- Windows 11, Python 3.11
- Ollama 설치됨. 2026-08-17 기준 생성에 쓰는 모델은 셋입니다 —
  질문·변형 `gemma4:latest` · 답변 `gemma4:12b` · **채점 `qwen3:4b`**(Apache 2.0, 계열이
  달라야 함 — 9-1). **임베딩에는 Ollama 를 쓰지 않습니다** — 로컬에서도 운영과 같은
  `models/bge-m3-onnx`(ONNX, MIT)를 씁니다(9-2). 그 밖에 `bge-m3:latest`,
  `embeddinggemma`, `qwen3-embedding:0.6b`, `nomic-embed-text`, `gemma4:e2b`,
  `gemma3:1b`, `qwen2.5-coder:7b`, `exaone3.5:7.8b`(NC라 사용 안 함)가 받아져 있습니다
- 로그는 저장소 밖 `C:/chatapp/logs/app.jsonl` 에 쌓습니다(`.env` 의 `LOG_FILE`). 5MB × 5개 회전.
  **코드 기본값은 `./data/logs/app.jsonl` 그대로 둡니다** — 운영은 Linux 라 절대경로를 박으면
  배포가 깨집니다. `data/question_log.jsonl`(질문 이력)은 로그가 아니라 데이터라 옮기지 않습니다

### 운영 (Azure AKS)

- **GPU 없음.** 폐쇄망 — GitHub만 연결됨
- **이 챗봇은 파드 하나입니다**(클러스터 전체가 파드 6개인 것과 혼동하지 마세요 —
  2026-08-17 확인). 그래서 `data/` 공유·세션 공유 문제가 없습니다
- 그 파드에 컨테이너는 **하나**입니다 — 임베딩을 앱 안에서(ONNX) 돌리기로 하면서 Ollama
  컨테이너가 없어졌습니다(9-2). 반입물도 이미지 하나(902MB)로 끝납니다
- `APP_MODE=serve` — LLM을 올리지 않습니다

### 개발 서버 (CentOS 7, Linux)

- 가용 메모리 7.7GB. 외부망 연결됨, 빌드 가능
- **인터넷에 노출돼 있고 SSH 실패 로그인이 2,600건 넘게 관측됨.**
  포트 노출·접근 제어를 반드시 고려하세요. `/admin` 이 무인증이던 전례가 있습니다

---

## 8. 임베딩 모델을 바꿀 때

현재 `bge-m3:latest` (1024차원). `embeddinggemma`(768차원, 절반 크기)로 바꿀 수 있습니다.

**차원이 달라지므로 반드시 재색인해야 합니다.** 안 하면 예외가 나거나, 더 나쁘게는
조용히 엉뚱한 결과를 줍니다. 컬렉션 메타데이터에 색인에 쓴 모델을 적어두고 다르면
예외를 던지게 해뒀습니다(`app/ingestion/vector_store.py`).

```bash
# .env 의 OLLAMA_EMBED_MODEL 변경 후
curl -u admin:비밀번호 -X POST "http://localhost:18100/api/admin/qa/reindex?include_docs=true"
```

모델별 **태스크 접두어**는 `app/ingestion/embedder.py` 에 표로 있습니다.
`bge-m3` 는 접두어를 붙이면 손해고 `embeddinggemma` 는 붙여야 합니다. 색인할 때와 검색할 때
같은 규칙을 써야 하며, 어긋나도 예외는 안 나고 검색 품질만 무너집니다.

**어느 모델이 나은지는 골든셋으로 재고 정하세요** (6번 ②).

### 8-2. 임베딩 모델 비교 실측 (2026-08-17) ★

문서 39개(청크 324개)에서 만든 **같은 문항 40개**로 세 모델을 재봤습니다.
재는 방식은 "질문을 던졌을 때 **그 질문이 나온 청크**가 상위에 오는가" 입니다 — 청크가 곧
정답이라 사람 라벨링이 필요 없습니다. 도구는 `scripts/compare_embedders.py` 에 있습니다.

| 모델 | 크기 | 파라미터 | 컨텍스트 | 차원 | Recall@1 | Recall@3 | MRR | 색인 | 검색 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `bge-m3` (그때까지 사용) | 1,104MB | 566.7M | 8192 | 1024 | 70.0% | 85.0% | 0.776 | 23.5초 | 254ms |
| **`embeddinggemma`** | **593MB** | 307.6M | 2048 | 768 | **72.5%** | **87.5%** | **0.804** | **18.1초** | **177ms** |
| `qwen3-embedding:0.6b` | 610MB | — | — | 1024 | 65.0% | 87.5% | 0.748 | 25.0초 | 185ms |

**성능만 보면 `embeddinggemma` 입니다** — 모든 지표가 `bge-m3` 이상이면서 크기는 절반,
검색은 30% 빠릅니다.

> ### 그런데 최종 채택은 **`bge-m3`** 입니다 (2026-08-17 확정) ★
>
> 이 앱은 여러 납품처에 설치하는 제품이라 **라이선스가 성능보다 앞섭니다**(9-1).
> `embeddinggemma` 는 Gemma Terms 라 납품처마다 조항·금지정책을 전달해야 하고, 그 의무가
> 최종 사용자까지 따라갑니다. `bge-m3` 는 MIT 라 그 부담이 없습니다.
>
> 성능 차이 2.5%p 는 40문항에서 **1건**이고, 아래 '재현성 주의'에 적었듯 1~2건 차이는
> 유의미하지 않습니다. **성능으로 진 것이 아니라 라이선스로 이긴 선택입니다.**
>
> 감수하는 것: 크기 1.1GB(2배) · 검색 254ms(77ms 느림). 파드 하나에 감당할 수준입니다.
> 얻는 것: 컨텍스트 8192(청크 길이 여유 5배 이상) · 널리 쓰여 ONNX 변환본을 구하기 쉬움.

> **재현성 주의.** 같은 문항·같은 모델인데 처음 잰 값과 1건(2.5%p) 차이가 났습니다
> (`embeddinggemma` 70.0 → 72.5). `bge-m3` 는 두 번 다 정확히 같았습니다. 위 표는
> `scripts/compare_embedders.py` 로 **세 모델을 같은 환경에서** 다시 잰 값이며, 이후 비교는
> 이 도구로만 하세요. 1~2건 차이는 유의미하지 않다고 보는 편이 안전합니다.

**벤치마크 순위가 또 뒤집혔습니다.** MTEB 최고점(64.34)인 `qwen3-embedding` 이 우리
데이터에서는 꼴찌(Recall@1 62.5%)였습니다. 1차의 3-6과 같은 일이 **두 번째**입니다.
모델 선택은 반드시 우리 문서로 재고 정하세요.

#### 이 숫자를 읽을 때 주의할 것

- **40문항은 적습니다.** 1건이 2.5%p라, "동률"은 "차이가 없다"가 아니라 **"이 표본으로는
  못 가른다"** 입니다. 그래서 최종 결정을 품질이 아니라 크기·속도로 했습니다 —
  문항이 바뀌어도 흔들리지 않는 근거입니다. 반대로 `qwen3` 탈락은 문항에 기댄 판단이라
  문항을 늘리면 뒤집힐 수 있습니다
- **문항은 `gemma4:latest` 가 문서에서 만든 것**이라 실제 사용자 말투와 다릅니다
- **고정 문항의 목적은 '비교 가능성'입니다.** 매번 새로 만들면 어제 숫자와 비교할 수 없고,
  고정본만 쓰면 그 문항에 맞춘 모델을 고르게 됩니다(과적합). 그래서 셋을 다 씁니다 —
  고정본(회귀 확인) · 새 문항(일반화 확인) · **실사용 로그(최종)**.
  `data/question_log.jsonl` 이 쌓이면 그것이 진짜 평가셋입니다
- 이번 측정은 **문서 검색**(관련 문서 안내) 기준입니다. 사용자가 실제로 부딪치는
  **QA 검색**(질문↔질문)은 승인 QA가 3건뿐이라 못 쟀습니다

#### 우리 문서의 성격 (모델을 고를 때 함께 보세요)

```
한글 41,215자 (63.4%)  ·  영문 23,785자 (36.6%)  ·  영문 낱말 960종
자주 나오는 것: API · apidev · YAML · Path · POST · URL
```

**한국어 전용 문서가 아닙니다.** 화면 경로·필드명·표기가 답변의 핵심이라, 한국어 특화
모델이 반드시 유리하다고 볼 수 없습니다. 한국어 특화 임베딩 모델은 대부분 Ollama 가 아니라
HuggingFace 에 있으므로, **ONNX 로 옮기면 그때 후보가 됩니다**(8-1). 같은 40문항으로
재면 모델당 15분입니다.

### 8-1. 임베딩을 Ollama 없이 — ONNX 검토 (2026-08-17, 결론 전)

운영에 남는 AI가 임베딩 하나뿐인데, 그것 때문에 **파드 안에 Ollama 컨테이너를 하나 더**
띄우고 있습니다. 임베딩을 앱 프로세스 안(ONNX Runtime)에서 돌리면 **컨테이너가 하나**로
줄고, 폐쇄망 반입도 이미지 하나로 끝납니다.

교체 지점은 `app/ingestion/embedder.py` **한 파일**입니다 — 처음부터 그렇게 잘라 뒀고,
테스트도 같은 자리를 `FakeEmbedder` 로 갈아 끼웁니다.

**대상 모델은 `bge-m3`(MIT)로 확정됐습니다**(8-2·9-1).

#### ONNX 변환본 조사 결과 (2026-08-17)

HuggingFace API 로 파일 목록과 크기를 직접 확인했습니다. **공식 저장소에 ONNX 가 있습니다.**

| 저장소 | 라이선스 | 파일 | 크기 |
| --- | --- | --- | --- |
| **`BAAI/bge-m3`** (공식) | MIT | `onnx/model.onnx` + `onnx/model.onnx_data` | **2,163MB** |
| `onnx-community/bge-m3-ONNX` | MIT | 양자화 여러 종 — `q4f16` 667MB · **`int8` 542MB** | 골라 씀 |
| `gpahal/bge-m3-onnx-int8` | MIT | `model_quantized.onnx` 단일 | **544MB** |

여기에 토크나이저(`tokenizer.json` 16.3MB + `sentencepiece.bpe.model` 4.8MB)가 더 붙습니다.

**주의 — ONNX 원본(FP32)은 지금보다 두 배 큽니다.** Ollama 의 GGUF 는 F16(1,104MB)인데
공식 ONNX 는 FP32(2,163MB)입니다. 그냥 옮기면 이미지가 커집니다. **양자화본(int8 542MB)을
써야** 컨테이너를 합치면서 크기도 줄어듭니다.

```
지금        Ollama GGUF F16   1,104MB  + Ollama 런타임(별도 컨테이너)
ONNX FP32   2,163MB           ← 이미지가 두 배로 늘어남
ONNX int8     542MB           ← 이쪽이라야 이득. 다만 품질 확인 필요
```

**int8 은 벡터가 달라집니다.** 같은 40문항으로 재서 적중률이 유지되는지 확인한 뒤에
정하세요(`scripts/compare_embedders.py` 는 Ollama 기준이라 ONNX 용 측정 경로가 필요합니다).

폐쇄망 반입은 **파일 3개 복사**로 끝납니다(모델 1 + 토크나이저 2). HF 공개 저장소라 인증도
필요 없습니다. 파이썬 쪽은 `onnxruntime` · `tokenizers` 휠을 함께 넣으면 됩니다.

#### ONNX(int8) 실측 (2026-08-17) ★

`gpahal/bge-m3-onnx-int8`(MIT) 을 받아 **같은 40문항**으로 쟀습니다. `DocIndex.embedder` 자리에
끼우기만 하면 되어 앱 코드는 건드리지 않았습니다.

| | Ollama GGUF F16 (이 PC = **GPU**) | ONNX int8 (**CPU**) |
| --- | --- | --- |
| Recall@1 | 70.0% | 67.5% (1건 차이) |
| Recall@3 | 85.0% | **85.0% (동일)** |
| MRR | 0.776 | 0.762 |
| **검색** | 254ms/건 | **35ms/건 (7배 빠름)** |
| 색인(324청크) | 23.5초 | 168.9초 (7배 느림) |
| 반입 크기 | 1,104MB + Ollama 런타임·컨테이너 | **565MB (모델+토크나이저)** |

**검색이 7배 빨라진 것이 핵심입니다.** GPU를 쓰는 Ollama보다 CPU ONNX가 빠른 이유는, 짧은
질문 한 건에서는 모델 연산보다 **HTTP 호출·서빙 오버헤드가 지배적**이기 때문입니다.
같은 프로세스 안에서 부르면 그 비용이 사라집니다. 운영에는 GPU가 없으므로 이 35ms 가
**운영 실제 값**입니다.

색인이 CPU에서 7배 느리지만(324청크 3분) 색인은 자주 하는 일이 아니라 감당할 만합니다.

##### 벡터는 "거의 같지만 같지는 않습니다"

같은 문장 20개를 두 방식으로 임베딩해 대조했습니다.

```
코사인 유사도  평균 0.9838 (0.9818 ~ 0.9875)   ← 0.99 미만
각 문장의 '가장 가까운 이웃'이 같은 경우: 20/20  ← 순위 구조는 보존
```

**절대값은 다르고 순위는 같습니다.** 그래서 Recall@3 은 동일하고 Recall@1 만 1건 움직였습니다.

> **혼용은 절대 금지입니다.** Ollama 로 색인하고 ONNX 로 검색하면(또는 그 반대) 0.984 짜리
> 어긋남이 그대로 검색 품질로 나타납니다. 예외는 안 나고 조용히 나빠지는 종류입니다.
> **전환하면 스튜디오·운영 양쪽 다 ONNX 로 바꾸고 전체 재색인**해야 합니다.

##### 배치 패딩 함정 — 옮기면서 실제로 밟았습니다 ★

처음 구현은 여러 문장을 **묶어서(패딩)** 임베딩했습니다. 통념대로 그게 빠를 줄 알았는데
두 가지가 망가졌습니다.

```
같은 문장을 단독 vs 배치로 임베딩   → 코사인 0.9877   (같아야 하는데 다름)
같은 문장을 단독 vs 단독            → 1.000000
청크 67개  배치 57.8초 / 한 건씩 10.4초   → 한 건씩이 5.6배 빠름
```

색인은 여러 청크를 묶고 검색은 한 건만 넣으므로, **정확히 같은 질문도 유사도가 0.987**
이 됐습니다(임계값 0.90 의 여유가 그만큼 줄어듭니다). 짧은 문장에 배치에서 가장 긴 문장
만큼의 계산을 시키는 것이라 속도도 손해였습니다.

**한 건씩 처리로 바꿨습니다.** 정확히 같은 질문이 다시 1.000 이 되고, 재색인이 164초에서
56초로 줄었습니다. `embed_batch()` 는 이름만 배치이고 안에서 하나씩 돕니다 — 이유는 그
함수 독스트링에 적어 뒀습니다.

##### 전환 후 최종 상태 (2026-08-17)

| | 전(Ollama F16 · GPU) | 후(ONNX int8 · CPU) |
| --- | --- | --- |
| Recall@1 / @3 | 70.0% / 85.0% | 67.5% / **85.0%** |
| 정확히 일치하는 질문 | 1.000 | **1.000** |
| 변형 질문 | 0.974 | 0.962 |
| 답 없어야 하는 질문 | 0.503 | 0.483 |
| **응답 시간** | 573~844ms | **21~43ms** |
| 재색인(39문서·324청크) | 23.5초 | 56초 |
| 컨테이너 | 챗봇 + Ollama | **챗봇 하나** |

Recall@1 1건 차이는 int8 양자화 손실이고, 8-2 의 기준대로 유의미하지 않다고 봅니다.
**응답이 20배 이상 빨라진 것**이 이 전환의 실질적 이득입니다.

##### 남은 확인

- `q4f16`(667MB) · FP32(2,163MB) 로 Recall@1 이 70.0% 로 돌아오는지 — int8 손실인지 확인
- 스튜디오에서 GPU(`onnxruntime-gpu`)를 쓰면 CPU 결과와 또 달라질 수 있음. 지금은 코드에서
  **CPU 로 고정**해 뒀다(`providers=["CPUExecutionProvider"]`). 색인이 정말 느려지면 그때
  GPU를 검토하되, **CPU 결과와 같은 벡터가 나오는지 먼저 재고** 결정할 것

**결론 전에 확인해야 하는 것 셋:**

1. **색인과 검색은 같은 구현이어야 합니다.** 색인은 스튜디오(개발 PC), 검색은 운영입니다.
   스튜디오는 Ollama, 운영은 ONNX 로 두면 **같은 `bge-m3` 라도 값이 미세하게 달라져**
   검색 품질이 조용히 흔들릴 수 있습니다. 바꾼다면 **양쪽 다** 바꾸는 것이 안전합니다
2. **벡터가 실제로 같은가.** 같은 문장 100개를 두 방식으로 임베딩해 비교하고, 탭 ⑦ 품질
   평가를 양쪽에서 돌려 적중률·오매칭률을 견줍니다. 다르면 전체 재색인이 필요합니다
3. **파드 메모리.** 파드가 하나라 모델도 한 벌이지만, 앱과 같은 컨테이너에 올라갑니다.
   양자화 여부에 따라 수백 MB ~ 2GB대입니다 — 파드 자원 한도와 함께 봐야 합니다

> 이 검토는 **2026-08-16 세션에서도 한 번 진행됐는데 기록이 남지 않아** 다시 시작하게
> 됐습니다. 결론이 나면 이 절에 숫자와 함께 적어 주세요.

---

## 9. 측정 기록 (2026-08-14)

문서 5건(청크 52개) + 검수 QA 3건 기준, 로컬 PC.

| 질문 | result_type | 유사도 | 응답 |
| --- | --- | --- | --- |
| API 등록은 어떻게 하나요? | `answer` | 1.000 | 2,997ms* |
| API 등록하는 방법 알려줘 | `answer` | 0.974 | 573ms |
| 권한그룹 뭐 골라요? | `answer` | 1.000 | 738ms |
| 템플릿 고치면 기존 API 영향 있나요? | `answer` | 0.945 | 798ms |
| API 그룹 코드 규칙이 있나요? | `related_docs` | 0.657 | 702ms |
| 점심 메뉴 추천해 주세요 | `unresolved` | 0.503 | 844ms |

\* 첫 호출은 임베딩 모델 로딩 포함.

**변형 질문("등록하는 방법 알려줘" → 0.974)이 걸리는 것이 핵심입니다.**
적중률은 사실상 변형 질문 목록이 결정합니다.

---

## 9-2. 파이프라인 첫 완주 (2026-08-17) ★ 실제 데이터로 한 바퀴

문서 39건으로 **생성 → 채점 → 반영 → 검수 → 답변 → 품질 측정**을 처음 끝까지 돌렸습니다.
그전까지는 코드만 있었고 아무도 실물로 통과시킨 적이 없었습니다.

### 결과

```
문서 39건 → 생성 16건 → 승인 7건 → 챗봇 응답 20~40ms → 품질 측정 완료
```

**적중률 Top-1 100% · 오매칭 0% · 미검색 34.2%** (문항 38개, 임계값 0.90)

| 임계값 | 답변율 | 적중 | 오매칭 |
| --- | --- | --- | --- |
| 0.75 | 100.0% | 100.0% | 0.0% |
| 0.80 | 94.7% | 94.7% | 0.0% |
| **0.85** | **94.7%** | **94.7%** | **0.0%** |
| 0.90 (지금) | 65.8% | 65.8% | 0.0% |
| 0.95 | 15.8% | 15.8% | 0.0% |

**0.90은 지나치게 보수적입니다.** 0.85로 낮추면 답변율이 30%p 오르는데 오매칭은 그대로입니다.
실패 사례도 대부분 0.898~0.899로 **0.001 차이**에 걸린 것들이었습니다.

> **오매칭 0%를 믿으면 안 됩니다.** 승인 QA가 7건뿐이라 서로 헷갈릴 상대가 없어서 나온 값
> 입니다. 게다가 평가 문항이 전부 "답이 있는 질문"이라, **답하지 말아야 할 때 답하는 경우는
> 측정 자체가 안 됩니다**(1차의 `is_confusion_test` 미이식). 손으로 던져본 반대말·없는 주제
> 질문 5개는 0.79 이하로 안전했지만, QA가 쌓이면 다시 재야 합니다.

### 이 한 바퀴가 찾아낸 결함 6개 ★

**코드 테스트 200개가 전부 통과하는 동안 숨어 있던 것들입니다.**

| # | 결함 | 어떻게 드러났나 |
| --- | --- | --- |
| 1 | 모델 불일치 검사가 **재색인 자체를 막음** | ONNX 전환 후 재색인 500 |
| 2 | 배치 패딩이 벡터를 흔듦 | 정확히 같은 질문이 0.987 |
| 3 | 판정 모델이 사고 과정만 뱉음 | 4점짜리 답변이 '판정 실패'로 버려짐 |
| 4 | 초안이 화면에 안 나옴 | "12건을 어디서 보나" |
| 5 | 반영해도 진행 현황 숫자가 그대로 | 새로고침해야 바뀜 |
| 6 | **품질 평가가 늘 0%** | 적중률 0%·미검색 100% |

**6번이 가장 컸습니다.** 검색 결과를 `qa_id` 단위로 접은 뒤에 '자기 자신'을 빼다 보니, 그 QA가
통째로 사라져 **어떤 데이터로 재도 0%가 나오는** 상태였습니다. 탭 ⑦의 숫자가 통째로
무의미했고, 실제 데이터로 처음 돌린 오늘에서야 드러났습니다.

전부 고쳤고 각각 테스트를 붙였습니다(총 204개). **다음 납품처에서도 코드 테스트만 믿지 말고
한 바퀴 돌려 보세요** — 이 여섯 개는 전부 그렇게만 나옵니다.

---

## 9-1. 모델 라이선스 (2026-08-17) ★ 납품 전에 반드시 확인

이 앱은 **한 프로젝트 전용이 아니라 여러 납품처에 설치하는 것이 목표**입니다. 그러면 모델
성능보다 **라이선스가 먼저**입니다. 점수가 조금 낮아도 제약이 없는 쪽이 낫습니다.

아래는 `ollama show <모델> --license` 로 **실제 텍스트를 확인한** 결과입니다.

| 모델 | 쓰는 곳 | 라이선스 | 납품 시 의무 |
| --- | --- | --- | --- |
| `bge-m3` | 임베딩(후보) | **MIT** | 사실상 없음 |
| `embeddinggemma` | 임베딩(선택됨) | Gemma Terms of Use | 사용 제한 조항을 **계약서에 포함** · Notice 파일 동봉 · 하위 배포자에게 전달 |
| `qwen3:4b` | 채점(선택됨) | **Apache 2.0** | 표시 의무만 |
| `qwen2.5-coder:7b` | — | Apache 2.0 | 표시 의무만 |
| `nomic-embed-text` | — | Apache 2.0 | 표시 의무만 |
| `gemma3:1b` | — | Gemma Terms of Use | 위와 같음 |
| `gemma4:latest` · `12b` | 질문·답변 | **Apache 2.0** | 표시 의무만 |
| ~~`exaone3.5:7.8b`~~ | 채점(제외됨) | **EXAONE 1.1 — NC** | ❌ **상업 사용 금지** |
| `qwen3-embedding:0.6b` | — | Ollama 에 표기 없음 | 원본에서 확인 필요 |

### exaone3.5 를 뺀 이유 ★

```
3.1 Commercial Use: The Licensee is expressly prohibited from using the Model,
Derivatives, or Output for any commercial purposes…
```

**출력물(Output)까지** 상업 이용을 금지합니다. 이 모델로 채점한 결과를 납품 제품에 쓰는 것
자체가 걸립니다. 2026-08-16 에 "계열이 다른 모델"이라는 **기술적 이유만으로** 골랐다가
라이선스 확인에서 걸러졌습니다 — 모델을 고를 때 라이선스를 함께 보세요.

### 대체 판정 모델: `qwen3:4b`

같은 시험(정확한 답 / 발췌에 없는 내용 / 질문과 무관)에서 **5점 · 1점 · 1점**으로 exaone 과
같게 갈랐고, 이유 문구의 한국어도 정확했습니다. 크기는 절반(2.4GB vs 4.6GB)이고 Apache 2.0
입니다. 답변 모델(`gemma4` 계열)과 계열이 달라 자기 채점 편향도 없습니다.

> `qwen2.5-coder:7b`(Apache, 이미 설치돼 있음)도 5·2·2 로 갈라내긴 했지만, 이유 문구의
> 한국어가 어색하고 근거 설명이 틀렸습니다. 검수자가 읽는 문구라 채택하지 않았습니다.
>
> `qwen3` 은 추론 모델입니다. `OLLAMA_THINK=false` 로 두고 있고 시험에서 빈 응답은 없었지만,
> 형식이 흔들리면 1차의 "추론 모델 빈 응답"을 먼저 의심하세요.

### Gemma 는 세대별로 라이선스가 다릅니다 ★

`gemma3:1b` 는 `Gemma Terms of Use` 인데 `gemma4` 는 `Apache License` 로 나옵니다. 표기 오류가
아니라 **실제로 다릅니다** — Gemma 4(2026-04 출시)부터 자체 라이선스에서 **표준 Apache 2.0**
으로 바뀌었습니다. 상업 이용·수정·재배포·판매에 제약이 없고 특허 라이선스도 포함됩니다.

그래서 **모델 이름에 'gemma' 가 들어간다고 같은 라이선스가 아닙니다.** 세대별로 확인하세요.

- `gemma4:*` (질문·답변) → **Apache 2.0** — 제약 없음
- `embeddinggemma` (임베딩) → Gemma 3 기반이라 **Gemma Terms** — 제약 있음

### 검토 범위는 "납품물에 무엇이 들어가는가"로 정해집니다

```
운영 이미지만 납품    → 임베딩 모델 1개만 (지금은 embeddinggemma → Gemma Terms)
스튜디오까지 납품     → 질문·답변·채점 모델까지 전부
사내에서만 QA 생성    → 스튜디오 모델은 사내 도구라 부담이 작음
```

**이것부터 정해야 어디까지 검토할지 정해집니다.** 아직 정해지지 않았습니다.

### 남은 제약은 임베딩 모델 하나뿐입니다 ★

판정 모델을 바꾸고 `gemma4` 가 Apache 로 확인되면서, **제약이 붙은 것은 `embeddinggemma`
하나만 남았습니다.** 그런데 하필 그것이 **운영 이미지에 들어가 모든 납품처에 배포되는**
유일한 모델입니다. 나머지(질문·답변·채점)는 스튜디오에만 있고 전부 Apache 2.0 입니다.

| 임베딩 후보 | 라이선스 | 재배포 의무 | 특허 조항 | Recall@1 | 크기 |
| --- | --- | --- | --- | --- | --- |
| `embeddinggemma` | Gemma Terms | **조항·금지정책 전달 필요** | 없음 | 72.5% | 593MB |
| `bge-m3` | **MIT** | 저작권 고지만 | 없음 | 70.0% | 1,104MB |
| (찾는 중) Apache 2.0 다국어 | Apache 2.0 | 고지만 | **명시적 포함** | ? | ? |

**2.5%p 를 얻는 대가로 납품처마다 계약 조항을 넣을 것인가**가 결정의 실체입니다.
`embeddinggemma` 는 상업 이용 자체는 자유롭지만, 납품처에 **Gemma Terms 전문과 금지 사용
정책(Prohibited Use Policy)을 전달**해야 하고 그 조항이 최종 사용자까지 따라갑니다.

**특허 조항은 Apache 2.0 에만 있습니다.** MIT 에도 명시 조항이 없으므로, 이 항목까지 챙기려면
Apache 2.0 인 임베딩 모델을 찾아야 합니다(`granite-embedding` · `gte-multilingual` 등,
아직 안 재봤습니다). 도구가 있으니 후보당 15분입니다(`scripts/compare_embedders.py`).

### 확정 (2026-08-17)

**임베딩은 `bge-m3`(MIT) 로 확정했습니다.** 아래가 근거입니다.

1. "라이선스가 먼저"라는 기준에 맞습니다 — 전달 의무도, 금지 사용 정책도 없습니다
2. **이미 쓰고 있어 바꾸는 비용이 0** 입니다. 재색인·재측정·문서 갱신이 필요 없습니다
3. 성능 차이 2.5%p 는 40문항에서 **1건**입니다. 8-2 에 적었듯 1~2건 차이는 유의미하지 않습니다
4. 손해는 크기(1.1GB vs 0.6GB)와 검색 77ms 뿐이고, 파드 하나에 감당할 수준입니다

Apache 2.0 다국어 임베딩 중에 `bge-m3` 를 넘는 것이 나오면 그때 갈아타면 됩니다.
`qwen3-embedding:0.6b` 는 이미 쟀고 65.0% 로 뒤졌습니다(4B·8B 는 크기가 목표와 안 맞습니다).
남은 미측정 후보는 `granite-embedding`(IBM, Apache) · `gte-multilingual`(Apache) 정도입니다.

> **`.env` 는 바꿀 것이 없습니다** — 처음부터 `bge-m3` 였습니다. 이 결정의 실행은
> "아무것도 바꾸지 않는 것"이고, 재색인도 필요 없습니다.
> `embeddinggemma` · `qwen3-embedding` 은 받아만 두었고 쓰지 않습니다(비교용).

---

## 10. 작업 규칙

- **git 커밋은 사용자가 직접 합니다.** `git add` / `git commit` 을 실행하지 마세요
- 새 화면을 만들 때는 컨트롤러·서비스·SQL도 새로 만듭니다 (기존 엔드포인트 재사용 ✕)
- 테스트는 `data/` 를 절대 건드리면 안 됩니다 — `tests/conftest.py` 의 autouse 격리 유지
- 응답 문체는 `~합니다`, 한국어

---

## 11. 참고 자료 위치

| 무엇 | 경로 |
| --- | --- |
| 퍼블 요청서 (챗봇) | `docs/퍼블요청/01_사용자_챗봇화면_퍼블요청서.md` |
| 퍼블 요청서 (관리자) | `docs/퍼블요청/02_관리자화면_퍼블요청서.md` |
| 퍼블 요청서 (로그인) | `docs/퍼블요청/03_로그인_퍼블요청서.md` — 02의 3번을 대체 |
| 퍼블 요청서 (챗봇 보완) | `docs/퍼블요청/04_챗봇화면_보완요청서.md` — 새 대화·답변 복사·맨 아래로 |
| 퍼블 요청서 (진행 현황) | `docs/퍼블요청/05_관리자_진행현황_및_메뉴개편_퍼블요청서.md` — 사이드바 10항목 |
| 퍼블 요청서 (작업 현황판) | `docs/퍼블요청/06_관리자_작업현황판_보완요청서.md` |
| 퍼블 요청서 (도움말 `?`) | `docs/퍼블요청/07_관리자_도움말툴팁_퍼블요청서.md` |
| 퍼블 요청서 (품질 레포트) | `docs/퍼블요청/08_관리자_품질레포트_퍼블요청서.md` |
| 퍼블 요청서 (주제 선택) | `docs/퍼블요청/09_챗봇_주제선택_보완요청서.md` — 2026-08-17 이식 완료 |
| 퍼블 요청서 (답변 피드백) | `docs/퍼블요청/10_답변피드백_퍼블요청서.md` — 2026-08-17 이식 완료 |
| 퍼블 요청서 (QA 가져오기) | `docs/퍼블요청/11_QA_가져오기_퍼블요청서.md` — 산출물 도착. 다만 1차 화면 위에 얹혀 와 재작업 요청 중 |
| ㄴ 재작업 | `docs/퍼블요청/11-1_QA가져오기_재작업요청서.md` + `11-1_기준파일/` — **아직 안 보냄** |
| 받은 매핑 문서 | `docs/퍼블요청/admin-mapping.md` — 관리자 화면 요소↔상태 |
| **이전 프로젝트 참고 소스** | **`참고/이전프로젝트/`** ← 저장소에 함께 넣었습니다 |
| ㄴ 질문 생성 · 평가 | `참고/이전프로젝트/eval/` |
| ㄴ 군집 분석 | `참고/이전프로젝트/pipeline/clustering.py` |
| ㄴ 배포 | `참고/이전프로젝트/deploy/` |

`참고/이전프로젝트/README.md` 에 **무엇을 가져다 쓰고 무엇을 버릴지** 정리해 뒀습니다.
이 폴더는 실행되지 않습니다 — `app/` 에서 import 하지 마세요. 구조가 바뀌어서
그대로 쓰면 안 되고, 설정 이름도 다릅니다(`rag_retrieval_floor` → `qa_match_threshold` 등).

### 이전 프로젝트 저장소도 함께 받으셨다면

`openapi-chat` 저장소의 **`작업내용.md` 를 꼭 읽으세요.** 거기 3번(겪은 문제와 해결)에
같은 함정을 다시 밟지 않기 위한 기록이 있습니다. 특히 이 작업들에 직접 영향을 줍니다.

| 이 문서의 할일 | `openapi-chat/작업내용.md` 에서 볼 것 |
| --- | --- |
| ① QA 생성 | 3-1(추론 모델이 빈 응답), 3-2(컨텍스트 창 초과로 조용히 잘림), 3-4(한국어 강제) |
| ② 품질 평가 | 3-6(**벤치마크 순위가 우리 데이터에서 뒤집힘**), 5번 표의 `llm_judge` 주의 |
| 답변 품질 전반 | 3-3(문서 베끼기·거부 문제), 3-5(모델이 아니라 **문서**가 원인이던 경우) |
| ⑤ 배포 | 3-10(볼륨이 이미지 data/를 가리는 문제 등) |

이전 프로젝트는 **완료가 아니라 대체된 것**입니다. 거기에 새 기능을 붙이지 마세요.
